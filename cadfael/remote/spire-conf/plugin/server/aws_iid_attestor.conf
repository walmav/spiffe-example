pluginName = "aws_iid_attestor" //needs to match the name used in plugin serverConfig

pluginCmd = "plugin/server/nodeattestor-aws_iid"
pluginChecksum = ""
enabled = true
pluginType = "NodeAttestor" //needs to match the handshake
pluginData {
	trust_domain = "example.org"
}
